services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: diego-music-backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    volumes:
      - ./temp:/tmp
    restart: unless-stopped
